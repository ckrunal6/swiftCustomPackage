struct MyLibrary {
    var text = "Hello, World!"
    var alertText = "Are you sure you want to create these Alert?"
    var customAlert = "how to use custom swift package manager in multiple porject"
}
